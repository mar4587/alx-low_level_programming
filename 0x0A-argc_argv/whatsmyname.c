#include <stdio.h>
/**
 * main - this program prints its name, followed by a new line
 *
 * argc: number of command line
 * argv: array contain line arg ..
 * Return (0)
 */
int main (int argc, char *argv[])
{
	printf("%s\n", 0, argv[0]);
	Return 0;
}
